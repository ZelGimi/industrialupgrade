package com.denfop.containermenu;

import com.denfop.blockentity.reactors.water.socket.BlockEntityMainSocket;
import net.minecraft.world.entity.player.Player;

public class ContainerMenuWaterSocket extends ContainerMenuFullInv<BlockEntityMainSocket> {

    public ContainerMenuWaterSocket(BlockEntityMainSocket tileEntityMainSocket, Player var1) {
        super(tileEntityMainSocket, var1);
    }

}
